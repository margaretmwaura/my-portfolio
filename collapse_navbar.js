jQuery(document).ready(function($) {
    $('#list li').click(function() {
       // let style =  $('.top-bar').getAttribute('style');
       //  $('.top-bar').setAttribute('style', style.replace('display: ', 'none'))
        console.log("we have clicked a menu")
    });
}(jQuery));
